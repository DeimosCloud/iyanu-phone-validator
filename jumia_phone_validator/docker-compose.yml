version: '2'
services:
    frontend:
         image: validator-frontend
         build:
          context: validator-frontend
          dockerfile: ../validator-frontend/Dockerfile
         ports:
           - "8080:8080"
       
    backend:
         image: validator-backend
         build:
          context: validator-backend
          dockerfile: ../validator-backend/Dockerfile
         ports:
           - "8080:8080"